[pytest]
testpaths = tests
markers =
    slow: marks tests as slow (full simulation)
    regression: marks regression tests for known bugs
    gate: marks thesis gate/promotion tests
    e2e: marks end-to-end lifecycle tests
addopts = -v --tb=short
